# Backlog / 打磨清单

上线前的打磨项与待决策。✅ 已做 · ⬜ 待办 · ❓ 需拍板

## 已做
- ✅ 封面墙返回不再刷新（sessionStorage 缓存）
- ✅ 搜索查询写进 URL，后退/前进/分享可恢复
- ✅ 搜作者能搜到作者本人（不只是含名字的书）
- ✅ 页脚 + 关于 / 联系 / 隐私页

## 待办（打磨）
- ⬜ **作者页重做**：现在全是列表、不好看、缺介绍。改成更像页面（头部信息 + 作品用封面卡片）。OL 作者简介稀疏，多数 bio 为空——靠众包补。
- ⬜ **作者重复**：OL 有多个同名作者记录（如三个"曹雪芹"）。需要作者去重/合并（可能半自动 + 人工）。
- ⬜ **整站导航**：各页"返回"统一、面包屑。

## 需拍板（大决策）
- ❓ **账号 / 审核员协作（#6）**：当前单一 admin 口令不可扩展。
  - 方案 B（推荐）：**只给审核员/管理员账号**（users 表 + 角色 admin/reviewer + 登录会话），贡献者仍匿名。多人协作 + 审计谁审的。
  - 方案 C：所有人都要账号（含贡献者）。门槛高，违背"低门槛贡献"。
- ❓ **标签系统（#7）**：当前 subjects 是自由文本、逗号输入、乱、难按 tag 索引。
  - 方案 B（推荐）：**标签做成实体**（tags 表 + work_tags 多对多）+ 按 tag 浏览页 + 输入用标签选择器（自动补全已有标签）。把现有 subjects 迁进去（去重归一）。

## 数据质量（Phase D 及以后）
- ⬜ **D：Google 富化**——89% 拼音标题转中文（懒补 A + 众包 C + 少量预补 B）。
- ⬜ 拼音作者名转中文译名（众包 + Google）。
